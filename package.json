{
  "name": "mounted-yaml-editor",
  "version": "1.1.0",
  "description": "Simple login-protected YAML editor for files in a mounted Docker folder.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "dependencies": {
    "yaml": "^2.8.4"
  }
}
